schema_version: 1

context:
  version: "0.18.4"

package:
  name: anycorn
  version: ${{ version }}

source:
  - url: https://pypi.org/packages/source/a/anycorn/anycorn-${{ version }}.tar.gz
    sha256: 262366e459d1631c2a2aee1018fa58fa4f7adc516e27d6cfbf4e1c2478f9d7ae

build:
  number: 0
  noarch: python
  script:
    - ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation --disable-pip-version-check

requirements:
  host:
    - pip
    - python ${{ python_min }}.*
    - uv-build
  run:
    - python >=${{ python_min }}
    - exceptiongroup >=1.1.0,<2.0
    - h11
    - h2 >=3.1.0
    - hpack
    - anyio >=4.0,<5.0
    - priority
    - tomli
    - typing_extensions
    - wsproto >=0.14.0
    - rich-click >=1.8.3,<2.0.0

tests:
  - python:
      imports: anycorn
      pip_check: true
      python_version: ${{ python_min }}.*
  - requirements:
      run:
        - python ${{ python_min }}.*
        - pip
    script:
      - anycorn --help

about:
  license: MIT
  license_file: LICENSE
  homepage: https://github.com/davidbrochart/anycorn
  summary: A fork of Hypercorn that uses AnyIO.

extra:
  recipe-maintainers:
    - davidbrochart
