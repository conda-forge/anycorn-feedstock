schema_version: 1

context:
  version: "0.18.5"

package:
  name: anycorn
  version: ${{ version }}

source:
  - url: https://pypi.org/packages/source/a/anycorn/anycorn-${{ version }}.tar.gz
    sha256: 6756c68460e2228d82d5f8ac01b5a4e02572ff6778dd0327649c4c574739aaec

build:
  number: 0
  noarch: python
  script:
    - ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation --disable-pip-version-check
  python:
    entry_points:
      - anycorn = anycorn.__main__:main

requirements:
  host:
    - pip
    - python ${{ python_min }}.*
    - uv-build
  run:
    - python >=${{ python_min }}
    - exceptiongroup >=1.1.0,<2.0
    - h11
    - h2 >=3.1.0
    - hpack
    - anyio >=4.0,<5.0
    - priority
    - tomli
    - typing_extensions
    - wsproto >=0.14.0
    - rich-click >=1.8.3,<2.0.0

tests:
  - python:
      imports: anycorn
      pip_check: true
      python_version: ${{ python_min }}.*
  - requirements:
      run:
        - python ${{ python_min }}.*
        - pip
    script:
      - anycorn --help

about:
  license: MIT
  license_file: LICENSE
  homepage: https://github.com/davidbrochart/anycorn
  summary: A fork of Hypercorn that uses AnyIO.

extra:
  recipe-maintainers:
    - davidbrochart
